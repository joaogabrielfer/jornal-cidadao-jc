<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Escrever Matéria - Painel de Controle</title>

    <style>
        /* Reset básico e Fonte Principal */
        * { font-family: Verdana, sans-serif; box-sizing: border-box; margin: 0; padding: 0; }
        
        body { 
            margin: 0; 
            background-color: #f8f9fa;
            color: #333;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* Cabeçalhos */
        .headerUol {
            background: rgb(38, 38, 38);
            top: 0;
            position: sticky;
            width: 100%;
            text-align: center;
            font-size: xx-small;
            height: 30px;
            z-index: 100;
        }
        .h1Uol { color: white; margin: 0; padding-top: 5px; }
        .headerJc {
            background: rgb(217, 39, 46);
            width: 100%;
            height: 130px; 
            position: relative;
            display: flex; 
            align-items: center; 
            justify-content: center; 
            flex-direction: column; 
        }
        .logoJc{ color: white; text-align: center; margin: 5px 0; }
        .nomeJc{ color: white; text-align: center; margin: 5px 0; font-size: 1.2em;}
        .headerNav {
            background: rgb(167, 166, 166);
            height: 40px; 
            display: flex;
            align-items: center; 
            padding: 0 15px; 
            margin: 0;
        }
        .headerNav nav ul {
            margin: 0;
            padding: 0;
            list-style: none; 
            display: flex; 
            gap: 15px; 
        }
        .headerNav nav a {
            text-decoration: none; 
            color: #333; 
            font-weight: bold;
            font-size: 14px;
            padding: 8px 16px; 
            border-radius: 5px; 
            background-color: rgba(255, 255, 255, 0.4); 
            transition: background-color 0.2s ease, color 0.2s ease; 
        }
        .headerNav nav a:hover {
            background-color: #333; 
            color: white; 
        }

        .mainContainer { 
            max-width: 1140px; 
            margin: 5rem;
            padding: 2rem; 
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            flex-grow: 1;
        }
        .pageTitle { 
            font-size: 1.75rem; 
            margin-bottom: 2rem; 
            border-bottom: 1px solid #eee;
            padding-bottom: 1rem; 
            color: #333;
        }
        
        /* Estilos para o Formulário */
        .form-group { margin-bottom: 1.5rem; }
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #555;
        }
        .form-group input[type="text"], .form-group textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 1em;
            color: #333;
            font-family: Verdana, sans-serif;
        }
        
        /* Botões */
        .btn {
            padding: 10px 16px;
            border-radius: 4px;
            border: none;
            cursor: pointer;
            font-weight: bold;
            font-size: 0.95rem;
            transition: filter 0.2s;
            color: #fff; 
        }
        .btn:hover { filter: brightness(0.9); }
        .btnSucesso { background-color: #28a745; }
        .btnSecundario { background: #6c757d; }
        .btnPerigo { background-color: #dc3545; }

        /* Enquete */
        #poll-options-container div {
            display: flex;
            gap: 10px;
            margin-bottom: 10px;
            align-items: center;
        }
        #poll-options-container input[type="text"] { flex-grow: 1; }
        hr { border: 0; border-top: 1px solid #eee; margin: 2rem 0; }
        
        /* Mensagem de Feedback */
        .feedback-message {
            padding: 15px;
            border-radius: 4px;
            text-align: center;
            font-weight: bold;
            margin-bottom: 1.5rem; 
        }
        .success {
            background-color: #d4edda;
            border: 1px solid #c3e6cb;
            color: #155724;
        }
    </style>
</head>
<body>

    <header class="headerUol">
        <h1 class="h1Uol">Barra da Uol</h1>
    </header>
    <header class="headerJc">
        <div>
            <h1 class="logoJc">Logo do Jc</h1>
            <h1 class="nomeJc">Sistema Jornal do Commercio de comunicação</h1>
        </div>
    </header>
    <header class="headerNav">
        <nav>
            <ul>
                <li><a href="/">Início</a></li>
                <li><a href="/charge">Charges</a></li>
                <li><a href="/admin">Admin</a></li>
            </ul>
        </nav>
    </header>
    <main class="mainContainer">
        <h2 class="pageTitle">Escrever Nova Matéria</h2>

        {{ if .message }}
            <div class="feedback-message success">{{ .message }}</div>
        {{ end }}

        <form id="materiaForm" action="/admin/api/materia" method="POST">
            
            <div class="form-group">
                <label for="title">Título:</label>
                <input type="text" id="title" name="title" required />
            </div>

            <div class="form-group">
                <label for="author">Autor:</label>
                <input type="text" id="author" name="author" required />
            </div>
            
            <div class="form-group">
                <label for="body">Conteúdo:</label>
                <textarea id="body" name="body" rows="15"></textarea>
            </div>
            
            <hr />

            <h3>Enquete (Opcional)</h3>
            <div class="form-group">
                <label for="poll_question">Pergunta da Enquete:</label>
                <input type="text" id="poll_question" name="poll_question" />
            </div>
            
            <label>Opções da Enquete:</label>
            <div id="poll-options-container"></div>
            
            <button type="button" id="add-option-btn" class="btn btnSecundario">+ Adicionar Opção</button>
            
            <hr />

            <button type="submit" class="btn btnSucesso">Publicar Matéria</button>
        </form>
    </main>

    <script>
        const container = document.getElementById('poll-options-container'); 
        const addBtn = document.getElementById('add-option-btn'); 
        
        addBtn.addEventListener('click', () => {
            const optionWrapper = document.createElement('div');
            
            const newInput = document.createElement('input');
            newInput.type = 'text';
            newInput.name = 'poll_options'; 
            newInput.placeholder = 'Texto da opção';

            const removeBtn = document.createElement('button');
            removeBtn.type = 'button';
            removeBtn.textContent = 'Remover';
            removeBtn.classList.add('btn', 'btnPerigo');
            removeBtn.onclick = () => {
                optionWrapper.remove();
            };

            optionWrapper.appendChild(newInput);
            optionWrapper.appendChild(removeBtn);
            container.appendChild(optionWrapper);
        });

        const form = document.getElementById('materiaForm');
        form.addEventListener('submit', async (event) => {
            event.preventDefault(); 
            
            const formData = new FormData(form);
            try {
                const response = await fetch(form.action, { method: 'POST', body: formData });
                
                if (response.ok) {
                    const successHtml = await response.text();
                    document.documentElement.innerHTML = successHtml;
                } else {
                    const errorHtml = await response.text();
                    document.documentElement.innerHTML = errorHtml;
                }
            } catch (error) {
                alert('Erro de conexão. Verifique sua rede e tente novamente.');
            }
        });
    </script>
    
</body>
</html>
